---
name: project-ap14-gst-engine
description: AP-14 GST engine — CLOSED 2026-06-12, all 3 units approved (compute service, split persistence, flag-sourcing); AP-15 is next story
metadata:
  type: project
---

**AP-14 (Epic 5 — GST computation service) is CLOSED as of 2026-06-12 — all 3 units human-approved.** Unit 3 ("Flag-sourcing in saveInvoiceDraft") was approved verbatim "yes commit all the changes and complete checkbox in notion", closing the story. The main conversation handled the git commit and Notion checkbox updates (not this orchestrator). Next story on the next invocation: **AP-15 — Live preview parity**, which drives the invoice preview/PDF/pay-page off the server totals AP-14 now persists, and owns the final cross-surface "figures match" verification.

**Why:** Unit 1 (pure `computeTotals` in `src/lib/invoice/compute-totals.ts` + 46-test suite) was human-approved 2026-06-12 and is correct, but on its own left three AP-14 acceptance criteria unsatisfied because the split was computed then dropped at the call site.

**Why:** AP-14's PRD checkboxes include "Intra-state → CGST=SGST; inter-state → IGST", "gst_enabled=false → zero tax", and "AC: figures match across preview/PDF/pay-page". At the `saveInvoiceDraft` call site (`src/app/(app)/invoices/actions.ts:86`) the flags are hardcoded `{ gstEnabled: false, isInterstate: false }`, and `lineItemsPayload` (lines 88-102) forwards only line_subtotal/line_tax/line_total — cgst/sgst/igst/round_off never reach the `save_invoice_draft` RPC, which has no params to receive them. So the split cannot be persisted or read by any downstream surface. These ARE AP-14's own checkboxes, not AP-15's.

**How to apply:** The remaining AP-14 work is two coupled units, sequenced backend-first: (1) backend-engineer extends `save_invoice_draft` RPC + invoice/line tables with cgst/sgst/igst/round_off persistence (Supabase-MCP evidence required); (2) call site wires gstEnabled from business.gst_enabled and isInterstate from supplier-vs-customer state_code comparison, and forwards the split into the RPC. Do NOT report AP-14 done until both land and "figures match across preview/PDF/pay-page" is verifiable. AP-15 (live preview parity) depends on this persistence existing.

**Unit 2 (persistence) — APPROVED 2026-06-12 ("yes approved").** Migration adds cgst/sgst/igst to invoice_line_items (invoices already had cgst/sgst/igst/round_off/is_interstate/gst_enabled columns). RPC rewritten to 14-arg signature (old 8-arg DROPped — only new sig live, only TS caller is actions.ts). Action forwards split + regime; SaveDraftResult/SavedDraftLine extended. Evidence: lint/tsc exit 0, migrations applied to dev (Option B authorized), 3-case round-trip + negative round_off, EXPLAIN confirms invoice_line_items_invoice_id_idx, advisors clean of new findings, GRANT/RLS verified. Reviewer PASS. All AP-14 work still uncommitted on main. NOTE: in the live app path all splits persist as 0 until Unit 3 flag-sourcing lands (action still hardcodes gstEnabled:false ~actions.ts:86). One open Medium (non-blocking): RPC p_gst_enabled defaults true vs action's conservative false — never hit in live path.

**Unit 3 (flag-sourcing) — APPROVED 2026-06-12 ("yes commit all the changes and complete checkbox in notion"), closing AP-14.** Single-file change to `src/app/(app)/invoices/actions.ts`: replaced hardcoded `{gstEnabled:false, isInterstate:false}` with two RLS-bound server reads — gstEnabled from `businesses.gst_enabled`, isInterstate from `businesses.state_code` vs `customers.state_code` (null-safe: either null/empty → false/intra-state, justified comment). Defensive logger.error+{ok:false} on DB error/missing row. lint+tsc clean, reviewer PASS zero findings, a11y n/a (no UI). DECISION baked: null state_code → intra-state CGST/SGST, never IGST (surfaced to user, not silently invented).

**AP-14 functionally complete in the live path.** All 6 PRD checkboxes satisfied: per-line split (Unit 1), intra/inter CGST=SGST vs IGST (Unit 1 compute + Unit 3 sources the regime live), gst_enabled=false→zero (Unit 3 sources it live), round-off (Unit 1), unit tests (Unit 1, 46 tests). LAST checkbox "AC: figures match across preview/PDF/pay-page" — persistence+compute now correct; cross-surface PARITY VERIFICATION belongs to **AP-15 (Live preview parity)**, which drives preview/PDF/pay-page from these server totals.

**How to apply (next invocation):** Status Sync reports AP-14 closed. **AP-15 — Live preview parity is now ALSO closed (2026-06-12)** — see [[project-ap15-live-preview-parity]]. AP-15 read the server-side cgst/sgst/igst/round_off totals to render the preview; parity AC was recorded as preview==persisted verified in-app, with the PDF + pay-page legs deferred to their own epics. The git commit + Notion checkboxes for AP-14 were handled by the main conversation, not this orchestrator.
